import CurrentBookings from "../../../components/CurrentBookings";

const UserDashboard = () => {
  return (
    <div>
      User Dashboard
      <CurrentBookings />
    </div>
  );
};

export default UserDashboard;
